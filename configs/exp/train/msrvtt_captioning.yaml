# @package _global_

defaults:
  - /train # define in code
  - /model@model: cocap
  - /dataset@train_dataloader.dataset: msrvtt
  - /dataset@val_dataloader.dataset: msrvtt
  - /exp/train/base
  - _self_

trainer:
  default_root_dir: "./logs/msrvtt_captioning"
